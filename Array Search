class Solution:
    def search(self, arr, x): 
        
        index=-1
        n=len(arr)
        for i in range(n):
            if arr[i]==x:
                index=i
                break
        return index 
